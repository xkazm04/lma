import { DocumentTranslationPage } from '@/app/features/documents/sub_Translation';

export const metadata = {
  title: 'Document Translation | LoanOS',
  description: 'Convert structured data into professional legal clause language',
};

export default function TranslatePage() {
  return <DocumentTranslationPage />;
}
